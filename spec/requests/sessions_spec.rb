require 'rails_helper'

RSpec.describe 'Sessions', type: :request do
  # describe 'POST /login' do
  #   let!(:player) { create(:player) }

  #   context 'with valid credentials' do
  #     it 'authenticates the player and returns a success response' do
  #       post '/login', params: { username:, password: 'password' }
  #       expect(response).to have_http_status(:success)
  #       expect(JSON.parse(response.body)).to include('token')
  #     end
  #   end

  #   context 'with invalid credentials' do
  #     it 'does not authenticate the player and returns an error response' do
  #       post '/login', params: { username: player.username, password: 'wrong_password' }
  #       expect(response).to have_http_status(:unauthorized)
  #     end
  #   end
  # end
end